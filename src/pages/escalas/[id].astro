---
import Layout from '../../layouts/Layout.astro';
import GlasgowComponent from '../../components/calculators/GlasgowComponent.vue';
import QsofaComponent from '../../components/calculators/QsofaComponent.vue';
import TraumaScoreComponent from '../../components/calculators/TraumaScoreComponent.vue';
import IndiceShocKComponent from '../../components/calculators/IndiceShocKComponent.vue';
import ParcklandComponent from '../../components/calculators/ParcklandComponent.vue';
import MadridDirectComponent from '../../components/calculators/MadridDirectComponent.vue';
import IndiceTraumaPediatricoComponent from '../../components/calculators/IndiceTraumaPediatricoComponent.vue';
import SuperficieQuemadaPediatricaComponent from '../../components/calculators/SuperficieQuemadaPediatricaComponent.vue';
import escalas from '../../data/escalas';

export const getStaticPaths = () =>
  escalas.map(({ id }) => ({ params: { id } }));

const { id } = Astro.params;
---

<Layout>
  <div class="container">
    {id === 'glasgow'       && <GlasgowComponent client:load standalone />}
    {id === 'qsofa'         && <QsofaComponent client:load standalone />}
    {id === 'trauma-score'  && <TraumaScoreComponent client:load standalone />}
    {id === 'indice-shock'  && <IndiceShocKComponent client:load standalone />}
    {id === 'parkland'      && <ParcklandComponent client:load standalone />}
    {id === 'madrid-direct' && <MadridDirectComponent client:load standalone />}
    {id === 'itp'             && <IndiceTraumaPediatricoComponent client:load standalone />}
    {id === 'scq-pediatrica'  && <SuperficieQuemadaPediatricaComponent client:load standalone />}
  </div>
</Layout>
